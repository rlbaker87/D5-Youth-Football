Drop your logo file here as:  rebels_logo.png

Recommended: square image (roughly 200x200px or larger), transparent
background if possible. The site already references this exact filename
in the header and homepage hero, so no code changes are needed once the
file is added — it'll just appear.
